{
  "name": "oneballer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "express": "^4.16.3",
    "flow": "^0.2.3",
    "request": "^2.88.0",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "@babel/plugin-transform-flow-strip-types": "^7.0.0-rc.1",
    "babel-cli": "^6.26.0",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.79.1"
  },
  "scripts": {
    "flow": "flow",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf build && mkdir build",
    "build-babel": "babel ./src --out-dir=./build --copy-files",
    "build-react": "cd client && npm run build && cp -r build ../build/client",
    "build": "npm run clean && npm run build-babel && npm run build-react",
    "start": "npm run build && node ./build/index.js"
  },
  "author": "",
  "license": "ISC"
}
